# OS cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db

# Editor / tooling
.claude/
.vscode/
*.swp
*~

# Logs / temp
*.log
.tmp/

# Node tooling deps (dev only — probe/budget)
node_modules/

# Wrangler local build cache (worker scaffold, not deployed — see wrangler.toml)
.wrangler/
